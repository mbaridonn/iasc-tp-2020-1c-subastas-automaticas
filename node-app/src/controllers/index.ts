import {addNewBid, getCurrentBids, updateBid, processNewOffer, notifyEndOfBid} from './bidsContoller'
import {addNewBuyer, getCurrentBuyers, updateBuyer} from './buyersController'


export = {
    addNewBid,
    addNewBuyer,
    getCurrentBids,
    getCurrentBuyers,
    updateBuyer,
    updateBid,
    processNewOffer,
    notifyEndOfBid
}